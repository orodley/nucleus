;; TODO: make this take varags and a format string
(extern rt_panic 1 -> panic!)
(extern rt_type 1 -> type)

(defun not (bool)
  (if bool false true))

(defun assert! (condition message)
  (if (not condition)
    (panic! message)))

(extern rt_rand 0 -> random)
